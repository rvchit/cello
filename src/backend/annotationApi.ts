// get image coordinates from viewport coordinates 
const viewportPoint = viewer.viewport.pointFromPixel(event.position);
const baseImagePoint = viewer.viewport.viewportToImageCoordinates(viewportPoint);

// translate viewport coordinates to image coordinates

// store base image coordinates / will be used for scaling annotation 

// coordinate mapping handler for annotation scaling

// create an abstraction layer but make it possible to save image caption pairs with the image 

//10x 100x100  10/5 = 1/2
//5x  50x50 

// dockerize frontend and backend and use docker compose to orchestrate them together 